#include <stdio.h>
#include<string.h>
int main(void)
{
//     int count = 0;
    char n[100];
    printf("enter the first name: ");
    gets(n);
    const char v[100];
    // printf("enter the last name: ");
    // gets(v);

    char*strcpy(char*n,const char*v);
    puts(v);

    // char f[200];
    // for (int i = 0; n[i] != '\0'; i++)
    // {
    //     f[i] = n[i];
    // }

    // for (int i = 0; n[i] != '\0'; i++)
    // {
    //     count++;
    // }

    // for (int i = 0; v[i] != '\0'; i++)
    // {
    //     f[count] = v[i];
    //     count++;
    // }
//     f[count]='\0';
//    puts(f);
}